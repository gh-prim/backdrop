/**
 * Emplacement du modal, vide par défaut.
 *
 * Next.js exige un `default` pour un slot parallèle: sans lui, un
 * rafraîchissement sur une page qui n'a pas d'équivalent dans le slot renvoie
 * un 404.
 */
export default function ModalDefault() {
  return null;
}
